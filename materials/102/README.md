# materials/102/

Course 102 materials live here.

At INITIAL stage this directory holds only the fixtures that labs reference:

- `figma-mock-frame.md` — fallback Figma frame used by the designer Lab 2
  `extract-figma-frame` skill when `FIGMA_ACCESS_TOKEN` is not set.

The Course 102 deck itself (slides + speaker notes) is built separately by
the `build-materials` skill and will land under `materials/102/deck/`.
